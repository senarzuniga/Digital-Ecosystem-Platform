# Digital-Ecosystem-Platform
The user needs a reliable technical prompt generation workflow for software and AI tasks. The solution should reduce ambiguity, preserve constraints, and provide reusable structured prompts. Environment: Local Streamlit prompt engineering workspace with JSON memory and automated critique loop
